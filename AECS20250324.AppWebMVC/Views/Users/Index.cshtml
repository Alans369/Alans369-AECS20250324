@model IEnumerable<AECS20250324.AppWebMVC.Models.User>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<input type="text" id="miInput" />

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Password)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@section Scripts {
    
    <script>
         $(document).ready(function() {
                    let temporizador;
        $('#miInput').on('input', function() {
            clearTimeout(temporizador);
            temporizador = setTimeout(function() {
                let textoBusqueda = $('#miInput').val();
                // Aquí realiza la llamada Ajax a tu servicio de búsqueda
                console.log("Buscando:", textoBusqueda);
            }, 300); // Retraso de 300 milisegundos


            $.ajax({
                url: '@Url.Action("Search")',
                type: 'POST',
                data: { valor: valorInput },
                success: function (value) {
                    Console.log(value)
                   
                }
            });
        });
        });


        


    </script>
}